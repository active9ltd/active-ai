export async function createReplicatePrediction(input:Record<string,unknown>){const token=process.env.REPLICATE_API_TOKEN;if(!token)throw new Error('REPLICATE_API_TOKEN is not configured');const version=process.env.REPLICATE_MODEL_VERSION;if(!version)throw new Error('REPLICATE_MODEL_VERSION is not configured');const r=await fetch('https://api.replicate.com/v1/predictions',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({version,input})});if(!r.ok)throw new Error(`Replicate request failed (${r.status})`);return r.json()}
