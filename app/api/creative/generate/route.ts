import {NextResponse} from 'next/server';import {generateWithOpenAI} from '@/lib/ai';
export async function POST(req:Request){try{const {format='AI Video',tone='Premium',prompt=''}=await req.json();const content=await generateWithOpenAI({system:'You are Active Ai, an expert performance advertising creative strategist. Return concise campaign-ready creative copy.',user:`Create a ${format} concept in a ${tone} tone. Brief: ${prompt||'Create a compelling product advertisement.'} Return: headline, hook, primary copy, CTA, visual direction.`});return NextResponse.json({creative:{format,tone,content}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Creative generation failed'},{status:500})}}
