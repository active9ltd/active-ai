'use client';
import { useState } from 'react';
export default function ProductForm(){const [url,setUrl]=useState('');const [loading,setLoading]=useState(false);const [result,setResult]=useState<any>(null);const [error,setError]=useState('');async function submit(){setLoading(true);setError('');setResult(null);try{const r=await fetch('/api/analyze-product',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Analysis failed');setResult(data.product)}catch(e){setError(e instanceof Error?e.message:'Analysis failed')}finally{setLoading(false)}}return <><section className="panel form-panel"><label>Product or website URL<input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://yourproduct.com" /></label><div className="form-actions"><button className="btn btn-primary" onClick={submit} disabled={loading||!url}>{loading?'Analysing…':'Analyse product →'}</button></div>{error&&<p style={{color:'#ff8e8e',marginTop:16}}>{error}</p>}</section>{result&&<section className="panel" style={{marginTop:18}}><p className="kicker">PRODUCT DETECTED</p><h2>{result.name}</h2><p className="muted">{result.description||'No description was found on the page.'}</p><div className="product-meta"><span>{result.hostname}</span><span>✓ Website analysed</span></div></section>}</>}
