import Link from 'next/link';
const campaigns=[['Summer Product Launch','Active','£428','4.83x'],['Spring Retargeting','Active','£312','3.91x'],['Coffee Acquisition','Paused','£184','2.72x']];
export default function Campaigns(){return <main className="dashboard-page"><div className="page-heading"><div><p className="kicker">CAMPAIGNS</p><h1>Your campaigns.</h1><p className="muted">Build, monitor and improve every campaign from one workspace.</p></div><Link className="btn btn-primary" href="/dashboard/campaigns/new">+ New campaign</Link></div><section className="panel"><div className="table-head"><span>CAMPAIGN</span><span>STATUS</span><span>SPEND</span><span>ROAS</span></div>{campaigns.map(([name,status,spend,roas])=><div className="table-row" key={name}><strong>{name}</strong><span className={status==='Active'?'status active':'status'}>{status}</span><span>{spend}</span><span>{roas}</span></div>)}</section></main>}
